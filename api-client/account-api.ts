import { ListParams, ListResponse } from '../models/api'

export const accountApi = {
  get(id: string): Promise<any> {
    return (async () => {
      return null
    })()
  },

  // getAll(params: Partial<ListParams>): Promise<ListResponse<>> {},
}
